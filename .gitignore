# Local GitHub Rulesets templates (import via UI; not tracked)
/rulesets/

# Cursor / IDE
.cursor/
.claire/
.claude/

# macOS
.DS_Store

# Never commit private keys
.env
chain/deploy/.env
chain/deploy/user-setup/private.local.env

# Node
node_modules/
chain/node_modules/
chain/deploy/node_modules/

# Hardhat (chain/)
chain/deploy/artifacts/
chain/deploy/cache/
chain/deploy/deployments/
chain/artifacts/
chain/cache/
typechain-types/
coverage/
coverage.json

# Legacy root artifacts (if Hardhat was run before chain/ move)
artifacts/
cache/

# Python
__pycache__/
*.pyc
.venv/

# Generated
passports/

# Temporary local nested repo (published: odp-android-companion)
android-companion/

# Local scratch / probes
.tmp-*/
tmp-*/
*.tgz

# Local HTTPS dev certs (openssl / mkcert)
web/.certs/

# Research notes are kept locally, not published. They are working material —
# vendor datasheets, prices, competitor analysis — and the decisions they support
# are recorded in commit messages and in the documents those decisions changed.
docs/research/

# Local deployment operator settings
chain/deploy/mainnet-spend-policy.local.json
chain/deploy-preflight.local.json

# Sealed ABI6 audit package: kept locally, too large for git. Integrity is recorded in
# review/ODP-0.7-ABI6-AUDIT.zip.sha256 and review/audit-handoff-verification.json.
review/ODP-0.7-ABI6-AUDIT.zip
review/audit-handoff-abi6/
