{
  "name": "object-digital-passport",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.10.0"
  },
  "description": "Object Digital Passport — Solidity, Hardhat, deploy (run from chain/ or npm run compile at repo root)",
  "scripts": {
    "compile": "hardhat compile && node deploy/scripts/eip170-report.mjs",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run deploy/scripts/deploy.js --network amoy",
    "deploy:mainnet": "hardhat run deploy/scripts/deploy.js --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "hardhat": "^3.16.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "overrides": {
    "diff": "^8.0.2",
    "lodash-es": "4.18.1",
    "serialize-javascript": "7.0.5",
    "js-yaml": "^4.3.1"
  }
}
