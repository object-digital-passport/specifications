# ODP 0.7: что должно измениться в приложении

Дата: 2026-09-24. Для отдельной сессии, которая работает в `apple-app` и `swift-core`.
Документ собирает в одно место все изменения версии 0.7, которые приложение обязано учесть.
Он заменяет разрозненные указания, но не переписывает их: подробности лежат по ссылкам.

Репозитории:

- приложение: `/Users/Andrei/Проекты/GitHub/Object Digital Passport/apple-app`;
- ядро: `/Users/Andrei/Проекты/GitHub/Object Digital Passport/swift-core`;
- спецификация: `/Users/Andrei/Проекты/GitHub/Object Digital Passport/specifications` (здесь пути даны относительно неё).

Правила для сессии приложения: начать с `apple-app/docs/CONTINUE_HERE.md`. Незакоммиченную работу
сохранять (там больше сотни изменённых файлов), не делать reset, stash или `git add -A`. Не разворачивать
контракты, не подставлять адреса 0.6 или старой 0.7, не использовать реальные ключи и не публиковать.

---

## 0. Сводка

### Что изменилось в 0.7 для приложения

1. Текущее поколение ABI — **`0.7-redesign-8`**. В нём новое поле `previewHash`: хеш облегчённой публичной копии основного фото.
2. Публичной становится только эта копия (JPEG до 1 МиБ, без GPS) и только по выбору человека. Оригиналы остаются в `.odpass`.
3. Публикация идёт в Arweave через Turbo. `passport.json` публикуется бесплатно. Копию фото оплачивает сам пользователь в POL или спонсор. Карты и IAP нет.
4. Кошелёк только внешний, через WalletConnect. Профиль C нельзя регистрировать на Safe или другую мультиподпись.
5. Газ спонсор оплачивает только пополнением: кнопка «Запросить POL», без QR.
6. QR бывает двух видов: `odp://<id>` и ссылка издателя `https://…?odp=<id>` со строгим разбором.
7. Перед пятью необратимыми действиями обязательны отдельные экраны-предупреждения.
8. Для мультиподписи учитывается граница UTC-месяца.
9. Сеть — только Polygon mainnet (137). Поколение `odp-0.7-redesign-8-polygon-20260925` развёрнуто 2026-09-25 и утверждено; адреса — SPEC §7 и `chain/generations.json`.

Всё, что было в [ODP_07_APP_HANDOFF.md](ODP_07_APP_HANDOFF.md) для `0.7-redesign-7` (прямой выпуск с
`operationId`, удалённый stop профиля, окна отзыва 72/24 часа, фиксация печати, журнал утверждений,
`.odpass`), остаётся в силе. Меняется только поколение ABI и добавляются пункты этого документа.

### Главная находка при чтении кода

Приложение ещё не перешло даже на `0.7-redesign-7`. Задание из `ODP_07_APP_HANDOFF.md` в коде не выполнено:

- `apple-app/Backend/Shared/Minting/RegistryMintCall.swift` кодирует старый tuple: в нём `dataUrl`/`imageUrl`, поле `mintAgent`, хвост `(bool,string)` и нет `operationId`;
- `apple-app/Backend/Shared/Chain/V07/CreatorProfileReader.swift` читает из `getCreator` пять полей вместе с `revokedAt`, а в ABI их четыре;
- `apple-app/Backend/Shared/Chain/V07/CreatorPassportRecord.swift` разбирает старые header (с `owner`), media (с URL) и classification (с `mintAgent`);
- `swift-core/Sources/ODPCore/Chain/ODPChain.swift` содержит адрес 0.6 и `contractVersion = 6`;
- `.odpass` приложения кладёт файлы в `originals/…`, а формат 0.7 требует `files/<sha256hex>`;
- удаления GPS, проверки `eth_getCode` и метода `personal_sign` в `WalletConnectService` нет.

Поэтому переход делать сразу на `redesign-8`. Промежуточный шаг на `redesign-7` не нужен.

### Источник истины

| Что | Где |
|---|---|
| Нормы | `SPEC.md`: §2–§3, §8, §9, §12, §15, §20, §21, весь §22 (CA-1…CA-18 и новые CA) |
| Интерфейсы | `chain/abi/*.json`, `chain/types/ethers-contracts/` |
| Схемы | `schema/passport-0.7.schema.json`, `schema/bundle-0.7/`, `schema/statement-0.7.schema.json` |
| Эталонный код | `chain/tools/passport.mjs`, `canonical.mjs`, `bundle.mjs`, `operations.mjs`, `edition-commitment.mjs`, `statement.mjs` |
| Поколение | `chain/generations.json` (сейчас `approvedGenerations` пуст) |
| Решения по безопасности | `review/usage-safety-abi6/CLOSURE.md`, раздел «Пересмотр 2026-09-24» |
| Проверка логики ABI7 | `review/logic-recheck-abi7/REPORT.ru.md` |

Если этот документ расходится с `SPEC.md` или ABI, прав `SPEC.md` и ABI. О расхождении сообщить владельцу.

**Состояние на 2026-09-24.** Вся линия 0.7 слита в `main` спецификации (PR #158, коммит `cf3213d`) и выпущена
пред-релизом `v0.7`. Бандл ABI `0.7-redesign-8` утверждён владельцем (`sha256:7392c821…a414d7`,
`review/v07-abi8-release/`). **Развёрнуто в Polygon mainnet 2026-09-25**, реестр `0x3281492981DCD492cc2F7c17398134e1D3B5CA1F`, все 10 адресов и хеши runtime — `chain/generations.json` и SPEC §7. Номера требований ниже сверены с `SPEC.md`:
хранение фото — §22.19 (CA-19.1–19.11), кошелёк и спонсор — §22.20 (CA-20.1–20.6), граница месяца для Safe —
CA-5.8–5.10 и CA-6.6, предупреждения — §22.18 (CA-18.1–18.7). Русский перевод SPEC — `docs/ru/SPEC.md`.
Исследования, на которые ссылается документ, лежат в `review/research-0.7/`.

### Что блокирует релиз

1. Поколение `redesign-8` развёрнуто и утверждено (2026-09-25), но приложение его ещё не знает: `CreatorRegistryPolicy.approvedDeployment` = `nil`. Включать запись в сеть только после пунктов 2–8, иначе приложение будет выпускать паспорта со старым кодировщиком.
2. Кодировщики и декодеры приложения не соответствуют ABI (раздел 1).
3. Журнал выпуска без `operationId` (раздел 2).
4. Нет экранов-предупреждений CA-18 (раздел 8).
5. Нет удаления GPS перед хешированием (CA-15.2, раздел 3).
6. Формат `.odpass` не совпадает с `bundle-0.7` (раздел 11).
7. Нет запрета C на Safe (CA-5.7, раздел 5).
8. Нет строгого разбора QR обоих видов (раздел 7).

Публикация в Turbo, «Запросить POL» и мультиподпись релиз профиля C не блокируют. Их можно выпустить следующим этапом.
Приложение пока работает только с профилем C (`AppModel+Registration.swift`: `guard type == "C"`).
Пункты про B/P/M и Safe нужны, когда появятся эти профили.

---

## 1. ABI `0.7-redesign-8`: кодировщики и декодеры

**Что изменилось.**

- `PassportMintInputs` получил поле `bytes32 previewHash` сразу после `imageHash`. Порядок: `core, dataHash, imageHash, previewHash, fileHash, anchorsHash, anchorTypesMask, editionCommitment`.
- `getPassportMedia` / `PassportMediaView`: `previewHash` тоже стоит после `imageHash`. Итог: `dataHash, imageHash, previewHash, fileHash, anchorsHash, anchorTypesMask, editionCommitment`.
- `previewHash = 0` значит «публичной копии нет».
- При ненулевом `previewHash` должно быть `imageHash != 0`, иначе `EC(142)`. И должно быть `previewHash != imageHash`, иначе `EC(143)`.
- Событие `PassportMinted` не меняется. `CONTRACT_VERSION` остаётся 7.
- Digest операции выпуска считается от всего tuple `m`, поэтому `previewHash` в него входит сам. Отдельно ничего добавлять не надо.
- Остальное — как в `redesign-7`: `mint*(PassportMintInputs, bytes32 operationId)`, `getCreator` из четырёх полей, `getPassportReleaseState`, `finalizePassportForPrint`, ошибки из таблицы SPEC §21.1.

**Требование.** SPEC §8, §13 («Use the compiled ABI»), §14 (байт версии 7 не различает ABI), CA-12.1.
Сигнатуры брать из `chain/abi/`, а не из этого текста.

**Где в коде.**

- `swift-core/Sources/ODPCore/Chain/ABI.swift`: проверить кодирование вложенных tuple и `bytes32`.
- `swift-core/Sources/ODPCore/Chain/ODPContract.swift`: `fetchPassport`, `media(id:)` (сейчас шесть полей с URL), `creatorId(forWallet:)`, `version()`, `frozen()`. Убрать то, чего в ABI нет.
- `swift-core/Sources/ODPCore/Chain/ODPPassportEvents.swift`: `PassportMinted` с `operationId` последним полем, `MintOperationCommitted`.
- `swift-core/Sources/ODPCore/Chain/ODPChain.swift`: адрес 0.6 и `contractVersion = 6` не должны попасть в путь 0.7.
- `apple-app/Backend/Shared/Minting/RegistryMintCall.swift`: переписать под новый tuple и `operationId`, убрать `Generation.legacyV6`/`.v7` с `mintAgent`.
- `apple-app/Backend/Shared/Chain/V07/CreatorPassportRecord.swift`: header без `owner`, media с `previewHash` и `editionCommitment`, classification без `mintAgent`.
- `apple-app/Backend/Shared/Chain/V07/CreatorProfileReader.swift`, `CreatorProfileGate.swift`: четыре поля, никакого `revokedAt`/`isRevoked` у профиля.
- `apple-app/Packages/ODPInfrastructure/Sources/ODPVerdict/Facts.swift`: `IssuerProfileState.revokedAt`/`stopped` описывают stop профиля, которого больше нет. Убрать из вывода проверки.
- `apple-app/Backend/Shared/Chain/CreatorRegistryPolicy.swift`: `Deployment.abiVersion: UInt8` не отличает `redesign-7` от `redesign-8` (у обоих 7). Добавить строковый `abiGeneration` и сверять его (SPEC §14, §7).

**Как проверить.**

1. Кодировщик выдаёт те же байты calldata, что `ethers` по `chain/abi/ObjectDigitalPassport.json` для фикстур physical, digital и mixed, с нулевым и ненулевым `previewHash`.
2. Декодер разбирает ответ `getPassportMedia` с локального Hardhat-узла и не принимает ответ старой формы.
3. Селекторы `mintPhysical/mintDigital/mintMixed` совпадают с ABI. Старый селектор с `(bool,string)` в коде не встречается.
4. Тест: `previewHash != 0` при `imageHash == 0` отклоняется ещё в приложении, до кошелька (это должно дать `EC(142)` на контракте). То же для `previewHash == imageHash` (`EC(143)`).

---

## 2. Журнал выпуска и `operationId`

**Что изменилось.** Ничего нового по сравнению с `redesign-7`, но в приложении это не сделано.

**Требование.** [ODP_07_APP_HANDOFF.md](ODP_07_APP_HANDOFF.md), раздел 1 и «Обязательная миграция»;
SPEC §2, §3 «Direct issuance and repeat protection», CA-5.1–5.3, CA-6.1–6.4.

Коротко:

1. Создать случайный ненулевой `operationId` и сохранить задание целиком до открытия кошелька.
2. Повтор после сбоя идёт с тем же `operationId` и теми же параметрами.
3. `AlreadyCommitted` после проверки namespace и receipt — это успех.
4. `MintOperationConflict` — остановка, ID не менять.
5. После смены UTC-месяца сначала выяснить судьбу старого задания через `getMintOperation`. Если оно не состоялось, переподготовить документ под новый месяц (новый `registeredAt`, новые хеши) с **тем же** `operationId`.

`previewHash` входит в задание. Решение «публиковать фото или нет» принимается до выпуска.
Если человек передумал после сохранения задания, это новое задание (CA-5.2). Добавить публичную копию к уже выпущенному паспорту нельзя, только новым паспортом.

**Где в коде.** `apple-app/Backend/Shared/Minting/CreatorMintRepository.swift`, `CreatorMintCoordinator.swift`,
`CreatorPassportDocument.swift`, `apple-app/Packages/ODPInfrastructure/Sources/ODPMintJournal/`, `ODPOperations/`.
Журнал уже хранит target/signer/hash и снимки. Нужно добавить `operationId`, поколение ABI и `previewHash`.

**Как проверить.** Сценарии из «Приёмки» `ODP_07_APP_HANDOFF.md`: перезапуск после отправки, повтор задания,
конфликт `operationId`, смена месяца, другой кошелёк или сеть. Векторы digest сверить с `chain/tools/operations.mjs`.

---

## 3. Облегчённая публичная копия фото

**Что изменилось.** Появилась необязательная публичная копия основного фото. Её хеш записывается в цепочку как `previewHash`.

**Требование** (SPEC §8, §9, §15; решение владельца 2026-09-24):

1. Копия делается только если человек выбрал «опубликовать фото». Иначе `previewHash = 0` и копии нет.
2. Формат: JPEG, не больше 1 048 576 байт, без GPS и других данных о месте и устройстве.
3. Перед публикацией человек видит превью именно этой копии, а не оригинала.
4. Копия лежит в `.odpass` рядом с оригиналом, как `files/<sha256hex>`.
5. В `passport.json` она описана якорем `{"type":"photo","data":{"role":"preview"},"hash":"sha256:<hex>"}`.
6. Такой якорь может быть только один и только при наличии якоря `photo` с `role: "primary"`.
7. `previewHash` в mint равен хешу этого якоря. Если якоря нет, `previewHash = 0`. Контракт этого не проверяет, проверяет приложение.
8. Если фото не публикуется, `.odpass` всё равно можно опубликовать. Фото тогда берётся из архива.
9. CA-15.2: перед хешированием найти GPS и данные устройства в **любом** фото (и в оригиналах тоже) и предложить удалить. После выпуска не подменять оригинал очищенной копией.
10. CA-15.1 и CA-18.3: экран выпуска показывает копию как файл, который станет публичным навсегда.

Схема уже требует: не больше одного якоря `photo` с `data.role: "preview"` и только при якоре с `role: "primary"`.
Якоря нет тогда и только тогда, когда `previewHash = 0`. `chain/tools/passport.mjs` дополнительно требует, чтобы хеш копии отличался от хеша основного фото.
Эталон для тестов приложения: `schema/vectors/physical-preview.*` и `schema/examples/0.7/physical-preview.json` (закоммичены в `main`).

**Проверка у читателя.** Копия проверяется по `previewHash`, оригинал — по `imageHash`. Копию показывать с подписью
«публичная копия основного фото», не как оригинал. Если есть только копия, писать «оригинал фото отсутствует» (CA-9.1).

**Где в коде.**

- `apple-app/Backend/Shared/Models/ImageStore.swift` и редактор фото (`CreatorPhotoEditor`): создание копии через ImageIO (`CGImageDestination`), удаление GPS/EXIF, подбор качества под 1 МиБ.
- `apple-app/Backend/Shared/Minting/CreatorPassportDocument.swift`: якорь `preview`, хеш, файл в архиве, поле mint.
- `apple-app/Frontend/iOS/Passport/MintFlowView.swift` и экран подготовки (`AddPassportView`): переключатель «опубликовать фото» и превью копии.
- `swift-core/Sources/ODPCore/Passport/Passport.swift`: модель якоря с `data.role`.

**Как проверить.**

1. Фото 12 МП с GPS даёт копию ≤ 1 048 576 байт без GPS. Проверить через `CGImageSourceCopyPropertiesAtIndex`.
2. Без публикации в `passport.json` нет якоря `preview` и `previewHash = 0`.
3. С публикацией ровно один якорь `preview`, его hash совпадает с `previewHash` и с байтами в `files/`.
4. Документ с двумя якорями `preview` или с `preview` без `primary` не проходит подготовку.
5. `chain/tools/bundle.mjs` принимает экспортированный архив.

---

## 4. Публикация и оплата хранения

**Что изменилось.** Появилась публикация в Arweave через Turbo. Раньше хранилище было не выбрано (SPEC §15: «Storage providers… remain undecided»).
Подробности и цены: `review/research-0.7/storage-research.ru.md` и `review/research-0.7/storage-payments.ru.md`.

**Требование** (решение владельца 2026-09-24; SPEC §22.19, CA-19.1–19.11):

1. `passport.json` публикуется бесплатно через Turbo. Бесплатный предел Turbo — 105 КиБ (`freeUploadLimitBytes: 107520`). Если документ больше, публикация становится платной, и об этом надо сказать.
2. Копия фото публикуется за POL самого пользователя:
   1. `eth_sendTransaction` через WalletConnect на адрес Turbo для POL;
   2. хеш транзакции передаётся в `POST https://payment.ardrive.io/v1/account/balance/pol`;
   3. кредиты зачисляются на тот же адрес `0x…`.
3. Каждую загрузку подписывает кошелёк пользователя через `personal_sign`: data item ANS-104, тип подписи 3 (Ethereum). Отправка: `POST https://upload.ardrive.io/v1/tx`.
4. Альтернатива: спонсор выдаёт адресу пользователя Credit Share Approval, приложение добавляет заголовок `x-paid-by: <адрес спонсора>`. Ключ спонсора приложению не нужен.
5. Адрес Turbo для оплаты закрепить в приложении и сверять с ответом `GET /v1/info`. Вслепую ответу не доверять.
6. IPFS CID вычисляется из хеша: CIDv1, кодек raw (`0x55`), multihash sha2-256 (`0x12 0x20` + 32 байта), base32 → `bafkrei…`. Это работает только для файлов не больше 1 МиБ, поэтому предел копии 1 048 576 байт.
7. Список шлюзов (ar.io и IPFS) хранить в настройках, а не зашивать один. `ipfs.io` и `dweb.link` выключены 21.09.2026.
8. После **любой** загрузки пересчитать SHA-256 скачанных байтов и сравнить с хешем из цепочки (CA-14.4). Не совпало — файл не показывать как подлинный.
9. Оплату картой (Stripe-checkout Turbo) и IAP в приложение не встраивать.
10. Перед публикацией предупредить: опубликованное удалить нельзя (CA-15.1, CA-15.4). Адрес пользователя станет `owner` каждого data item. Credit Share публично связывает спонсора с пользователем.
11. Если публикуется `.odpass` целиком, в нём оригиналы фото. Архив почти наверняка больше 105 КиБ, то есть платный. Показать, что станет публичным (CA-15.1).

**Swift SDK у Turbo нет.** Писать самим, по оценке из исследования 300–500 строк:

- сериализация ANS-104: заголовок типа 3, `owner` — 65-байтный несжатый публичный ключ, теги в Avro, deep-hash на SHA-384;
- `owner` входит в подписываемые данные. Публичный ключ получить один раз: подпись фиксированного текста и восстановление через `secp256k1` (в `WalletConnectService` уже есть `recoverPubKey`). Ключ не секретный, его можно хранить;
- HTTP-клиент: цена `GET /v1/price/bytes/{n}`, бесплатный остаток `GET /v1/account/free?address=`, баланс, загрузка, пополнение, проверка approvals `GET /v1/account/approvals`.

Тестовые векторы ANS-104 взять из `arbundles` (TypeScript).

**Риск App Review 3.1.1.** Ревьюер может посчитать оплату криптовалютой разблокировкой функции через криптокошелёк. Прецедента нет. Меры:

1. Бесплатный путь есть всегда: `passport.json` бесплатно, `.odpass` можно передать файлом, спонсор.
2. Платёж идёт сети Arweave через Turbo, а не разработчику. Приложение ничего не продаёт и ничего не получает.
3. Экран пополнения называет получателя (Turbo), сумму в POL и комиссию. Слов «купить у ODP» нет.
4. В App Store Connect → Review Notes объяснить: платёж — оплата стороннего хранилища из внешнего кошелька, как газ при выпуске, приложение ничего не продаёт.
5. Оставить флаг, который скрывает только кнопку «Пополнить кредиты». Публикация бесплатно и через спонсора при этом продолжает работать.

Второй риск: `personal_sign` над 48 байтами deep-hash кошелёк покажет как непонятный hex. Перед подписью приложение объясняет, что подписывается. Совместимость с Tangem и другими кошельками не проверена.

**Где в коде.** Новые модули в `apple-app/Packages/ODPInfrastructure` или `apple-app/Backend/Shared/Publish/`: `ANS104`,
`EVMPublicKeyCache`, `TurboClient`, `IPFSAddress`, `PublicFetchVerifier`, `PublicationRecord`.
Кода для IPFS и Arweave в приложении сейчас нет. Метод `personalSign` добавить в
`apple-app/Backend/Shared/Wallet/WalletConnect/WalletConnectService.swift` рядом с `sendTransaction`: с тем же дедлайном, проверкой сессии и сверкой ответа.
Namespaces менять не надо, `personal_sign` там уже есть.

**Как проверить.**

1. Data item, собранный в Swift, побайтно совпадает с `arbundles` на одинаковых входах и проходит его `verify`.
2. CID для строки `hello world` равен `bafkreifzjut3te2nhyekklss27nh3k72ysco7y32koao5eei66wof36n5e`.
3. Загрузка `passport.json` ≤ 105 КиБ проходит без баланса. Не выполнять на реальном кошельке без разрешения владельца.
4. Скачанный с двух разных шлюзов файл проходит сверку хеша. Подменённый ответ шлюза (mock) отклоняется.
5. Флаг «скрыть оплату» убирает кнопку пополнения и оставляет бесплатную и спонсорскую публикацию.

---

## 5. Кошелёк

**Что изменилось.** Решение окончательное: приложение не создаёт и не хранит кошельки (App Store 3.1.5(a),
разработчик — физлицо). Встроенного кошелька и Safe, который создаёт приложение, не будет. Только WalletConnect.

**Требование.**

1. `WalletVault` и `WalletService` остаются только в `#if DEBUG`. В Release их нет (сейчас так и есть, проверить после правок).
2. CA-5.7: профиль C нельзя регистрировать на Safe или другую мультиподпись. Приложение отказывается и регистрировать, и действовать как издатель C для такого адреса.
3. CA-5.7: для B/P/M Safe поддерживается во всех действиях роли. При регистрации B/P/M приложение рекомендует Safe с двумя и более владельцами.
4. CA-1.1, CA-1.2: сказать, что ODP не восстановит профиль. Не просить seed-фразу и не отмечать резервную копию как проверенную.

**Проверка C на мультиподпись.** По коду: проверки `eth_getCode` нет (`grep` по `apple-app` ничего не находит).
В `AppModel+Registration.swift` есть только `guard type == "C"`. Предлагаемое правило:

1. Перед регистрацией C и перед каждым действием издателя C вызвать `eth_getCode(address, "latest")`.
2. Пустой код — обычный аккаунт, продолжать.
3. Непустой код — отказать с объяснением: «Профиль C личный; для контракта-кошелька или мультиподписи выберите B, P или M».
4. Сбой RPC — «не удалось проверить», не разрешать.

Открыт вопрос с EIP-7702: код такого аккаунта начинается с `0xef0100`. См. «Открытые вопросы».

**Где в коде.** `apple-app/Backend/Shared/AppState/AppModel+Registration.swift` (`registerProfileOnChain`, `adoptOnChainProfile`),
`apple-app/Backend/Shared/Chain/V07/CreatorProfileGate.swift` (новое решение «контракт-кошелёк»), `CreatorRegistryRPC.swift` (метод `code(at:)`),
`CreatorMintCoordinator.swift` (проверка перед выпуском).

**Как проверить.** На локальном Hardhat-узле с Safe из `chain/deploy/test/ODP07Safe.test.js`: адрес Safe получает отказ
в регистрации C, обычный аккаунт проходит, ошибка RPC блокирует.

---

## 6. Спонсорская программа (университет, издатель): газ и публикация

**Что изменилось.** Спонсор — университет, музей, бренд — может оплачивать людям сетевые комиссии (газ) и публикацию
фото. Серверов ODP нет, встроенного кошелька нет (App Store 3.1.5(a), CA-20.1), relayer'а нет (CA-20.2). Поэтому в
0.7 спонсор платит только двумя способами, и оба не дают ему доступа к кошельку человека:

1. **Газ** — переводит небольшие суммы POL со своего кошелька на адреса людей (CA-20.2).
2. **Публикация фото** — выдаёт адресам людей право тратить свои кредиты Turbo (Credit Share Approval с лимитом
   и сроком); приложение при загрузке ставит заголовок `x-paid-by: <адрес спонсора>` (CA-19.9, раздел 4).

### Роли

| Кто | Что делает | Чего не может |
|---|---|---|
| Человек (студент) | Ставит свой кошелёк, подключает его к ODP через WalletConnect, нажимает «Запросить POL», сам подписывает регистрацию и выпуск | — |
| Приложение ODP | Показывает спонсора, отправляет запрос, показывает баланс, предупреждает о приватности | Создавать или хранить ключи, подписывать за человека |
| Спонсор | Держит свой кошелёк с POL, принимает запросы на своём адресе приёма, пополняет адреса, по желанию выдаёт кредиты Turbo | Подписывать за человека, забрать профиль или паспорта; он может только перестать платить |

### Поток «Запросить POL» (CA-20.3–20.6)

1. Человек получает от спонсора **ссылку спонсора** (письмо, сайт, чат) и открывает её. Приложение показывает
   название спонсора, его адрес кошелька и адрес приёма запросов, и просит подтвердить. QR внутри приложения не
   показывать и не сканировать.
2. Перед первым запросом — экран (CA-20.5): «<Спонсор> увидит адрес вашего кошелька и все его публичные действия.
   Доступа к кошельку он не получает и может только перестать пополнять».
3. Кнопка «Запросить POL». Приложение просит кошелёк подписать `personal_sign` текст:
   ```
   ODP gas request v1
   wallet: <адрес человека, 0x…, 42 символа>
   sponsor: <адрес спонсора>
   chainId: 137
   time: <UTC ISO 8601, до секунды>
   ```
4. `POST` на адрес приёма спонсора, тело JSON: `{"format":"odp-gas-request-v1","wallet":"0x…","sponsor":"0x…",
   "message":"<текст выше>","signature":"0x…"}`. Больше ничего: ни паспортных данных, ни идентификаторов устройства.
   Транзакций приложение не отправляет.
5. Адрес приёма не задан, не отвечает или отказал — открыть системное «Поделиться» с адресом кошелька текстом
   (CA-20.4). Резервного сервера ODP нет.
6. Экран кошелька показывает баланс POL и сколько примерно операций он покрывает: газ из `estimateGas` и текущая
   цена газа, не константа (CA-20.6). Ни одно действие не блокируется из-за отсутствия спонсора: человек всегда
   может пополнить кошелёк сам.

Формат текста и тела запроса пока не закреплён в SPEC: это предложение для первой реализации. Когда появится первая
реализация у спонсора, формат нужно внести в SPEC §22.20, чтобы любой спонсор мог принимать запросы от любого клиента.

### Что нужно на стороне спонсора (не в приложении)

Это отдельная работа, не для сессии приложения, но без неё программа не заработает:

1. Кошелёк спонсора с POL. Для организации — Safe с несколькими владельцами (CA-5.7).
2. Адрес приёма запросов: простой HTTPS-обработчик, который проверяет подпись (восстанавливает адрес из
   `signature` и сравнивает с `wallet`), время (не старше, например, 10 минут), `sponsor` и `chainId`, и ставит
   адрес в очередь. Можно и без сервера: форма или таблица, куда человек вставляет адрес через «Поделиться».
3. Скрипт пополнения: раз в N минут берёт очередь или список, проверяет баланс каждого адреса и доливает до
   порога (например, до 0,5 POL), с лимитом на адрес в месяц. Рекомендация: положить эталонный скрипт в
   `specifications/tools/` отдельной задачей.
4. По желанию — кредиты Turbo и Credit Share Approval для оплаты публикации фото.
5. Честное предупреждение для своих людей: спонсор видит адреса и их публичные действия; запись Credit Share в
   Arweave публично связывает спонсора с адресом.

### Где в коде

- Экран спонсора и кошелька в `apple-app/Frontend/iOS/` (профиль или кошелёк); обработка ссылки спонсора —
  рядом с обработкой `odp://`.
- Баланс и `estimateGas` через `PolygonRPC` (`swift-core/Sources/ODPCore/Chain/PolygonRPC.swift`).
- Подпись через новый `personalSign` (раздел 4).
- Хранение данных спонсора (название, адрес кошелька, адрес приёма) — локально, без синхронизации с сервером ODP.

### Как проверить

1. Подпись запроса восстанавливается в адрес кошелька; текст совпадает с шаблоном побайтно.
2. В теле запроса нет ничего, кроме полей из шага 4.
3. Без адреса приёма или при ошибке открывается «Поделиться» с адресом.
4. Экран CA-20.5 показывается до первого запроса и не пропускается.
5. Оценка числа операций меняется вместе с ценой газа.
6. Регистрация и выпуск работают с балансом, пополненным самим человеком, без спонсора.

---

## 7. QR и ссылки издателя

**Что изменилось.** Кроме `odp://<id>` разрешена ссылка издателя `https://…?odp=<id>`.

**Требование.** SPEC §12, CA-14.1–14.8, §22.10 (формат `.well-known/odp.json`).

1. Принимать `odp://<passportId>` и ссылку издателя. Любой другой `https://` — «не метка ODP» с причиной.
2. Ссылка издателя верна, только если: схема `https`; нет user-info; в query ровно один параметр с именем ровно `odp`; его значение после percent-decoding точно совпадает с форматом ID из §2 (заглавные буквы, без пробелов). `http`, повтор `odp` и `odp` во фрагменте отклоняются.
3. Путь, другие параметры и фрагмент на проверку не влияют.
4. ID проверяется по встроенному реестру (§22.12). Сайт не открывается ни для проверки, ни автоматически после сканирования.
5. Хост показывается точно, не-ASCII в punycode (CA-10.3). Отдельно от проверки паспорта: «ссылка ведёт на домен издателя», только если хост равен домену из `ODPProfileDirectory` издателя или его поддомену, и `https://<домен>/.well-known/odp.json` перечисляет этот профиль для этого поколения. Иначе «ссылка ведёт на <host>; издатель опубликовал <domain>» или «издатель не опубликовал домен».
6. Совпадение домена не означает подлинность предмета (§22.16). Несовпадение не меняет остальные проверки.
7. «Открыть сайт издателя» — отдельное действие с показом хоста (CA-14.3).
8. `.well-known/odp.json` скачивать без cookies и referrer, с ограничениями CA-13.2 и CA-14.4.

**Где в коде.**

- `apple-app/Backend/Shared/AppState/AppModel+NFC.swift`, `handleScannedCode` (строка ≈332): сейчас принимает только префикс `odp://`. Вынести разбор в отдельный строгий парсер.
- `apple-app/Frontend/iOS/ODPApp.swift`, `onOpenURL`: тот же парсер.
- `apple-app/Packages/ODPInfrastructure/Sources/ODPVerdict/`: сверка домена. Сейчас там читается `compromised` из `.well-known/odp.json`, а в SPEC §22.10 такого поля нет.
- Служебные payload вида `odp://nfc-provision?request=…` (`WorkspaceNFCProvisioningView.swift`) и `odp://<profileId>` (`ShareOverlayView.swift`) не должны разбираться как паспорт.

**Как проверить.** Табличный тест парсера: `odp://ODP-2026-09-123456789` принят; `https://a.example/p?odp=ODP-2026-09-123456789&x=1` принят;
`http://…`, `?odp=…&odp=…`, `#odp=…`, `?ODP=…`, `?odp=odp-2026-…`, `https://user@host/…`, GS1-ссылка — отклонены с причиной.
Mock `.well-known`: совпадение, поддомен, look-alike домен, недоступный сервер, файл больше 1 МиБ.

---

## 8. Обязательные предупреждения перед необратимыми действиями

**Что изменилось.** Новый раздел SPEC §22.18.

**Требование.** CA-18.1–18.7.

1. Отдельный экран подтверждения перед каждым действием: регистрация профиля, выпуск, фиксация печати, отзыв паспорта, открытие тиража.
2. Экран нельзя закрыть тапом мимо. Кнопка называет действие («Выпустить навсегда»), а не «OK». Ничего не выбрано заранее в пользу продолжения.
3. Регистрация (CA-18.2): тип профиля навсегда; профиль навсегда привязан к этому кошельку; ODP не восстановит, не заблокирует и не перенесёт его; потеря доступа к кошельку прекращает выпуск под этим ID.
4. Выпуск (CA-18.3): список всего, что станет публичным навсегда (CA-15.1), включая копию фото; карточку нельзя изменить; окно отзыва своей роли с точным временем окончания (C — 72 часа, B/P/M — 24 часа).
5. Фиксация печати (CA-18.4): после неё паспорт нельзя отозвать, даже если печать не удалась или файл потерян.
6. Отзыв (CA-18.5): отзыв нельзя отменить; паспорт и история остаются читаемыми; хеш причины становится публичным.
7. Резервная копия (CA-18.6): постоянное напоминание на экране профиля и на каждом подтверждении выпуска. Не писать, что копия проверена.
8. CA-18.7: у этих предупреждений нет «больше не показывать». Для пачки в одном задании предупреждение о выпуске можно показать один раз. Другие диалоги не копируют их стиль.

**Где в коде.** Новый общий компонент экрана подтверждения в `apple-app/Frontend/Shared/`; вызовы из регистрации
(`AppModel+Registration.swift` и её экрана), `MintFlowView.swift`, будущих экранов фиксации печати и отзыва.

**Как проверить.** UI-тесты: экран нельзя закрыть свайпом или тапом мимо (`interactiveDismissDisabled`); кнопка подписана действием;
нет переключателя «не показывать»; время окончания окна совпадает с `getPassportReleaseState().revocationDeadline` на mock.

---

## 9. Окно отзыва и фиксация печати

**Что изменилось.** Ничего нового по сравнению с `redesign-7`, но в приложении этого нет.

**Требование.** SPEC §8, CA-2.1–2.4, CA-6.5, CA-8.1–8.2; [ODP_07_APP_HANDOFF.md](ODP_07_APP_HANDOFF.md), раздел «Решение пользователя: stop удалён».

1. Окно отзыва показывать по роли: C — 72 часа, B/P/M — 24 часа, включительно. Не показывать одно окно 72 часа для всех.
2. Читать `getPassportReleaseState(id)`, показывать `revocationDeadline` и `printFinalizedAt` отдельно.
3. Готовый к печати файл выдавать только после подтверждённой фиксации, неотозванного статуса и проверенного экспорта архива. Для тиражей ещё и после открытия тиража.
4. `PassportPrintFinalized()` при отзыве — это «паспорт зафиксирован для печати», а не `EC(132)`.

**Где в коде.** `apple-app/Backend/Shared/Chain/PassportChainStates.swift`, `CreatorPassportRecord.swift`, экран паспорта `Frontend/iOS/Passport/PassportDetailView.swift`, экспорт этикетки.

**Как проверить.** Границы окна ровно в `mintTimestamp + 259200` (C) на локальном узле; до фиксации экспорт этикетки недоступен, превью помечено как непригодное.

---

## 10. Граница UTC-месяца для мультиподписи

**Что изменилось.** Найдено в `review/logic-recheck-abi7/REPORT.ru.md`, п. 2. Требования: CA-5.8–5.10, CA-6.6.

Суть: `mint*` и `submitProof` требуют текущий UTC-месяц (`EC(68)`). Предложение Safe создано в конце месяца,
последний владелец подписал после 00:00 UTC первого числа, и внутренний вызов откатывается. Safe v1.4.1 при
`safeTxGas = 0` и `gasPrice = 0` откатывает всю транзакцию, nonce не тратится. Все следующие предложения этого Safe
стоят, пока кто-то не исполнит отклоняющую транзакцию с тем же nonce.

**Требование.**

1. Предупреждать, если предложение с `year/month` создаётся меньше чем за 24 часа до конца UTC-месяца (CA-5.8). N сверить со SPEC.
2. Предложение, у которого сменился месяц, показывать как неисполнимое, а не как ожидающее (уточняет CA-5.6).
3. Предложить отклоняющую транзакцию с тем же nonce Safe. Объяснить, что без неё очередь Safe стоит.
4. Переподготовить задание под новый месяц с тем же `operationId`: он не был зафиксирован, потому что вызов откатился.
5. Перед переподготовкой проверить `getMintOperation` (или `proofOperations`), что задание действительно не состоялось.
6. Для B напомнить: медленные co-signer'ы могут не успеть отозвать паспорт в окно 24 часа.

К профилю C это не относится: C на Safe запрещён (раздел 5). Делать вместе с поддержкой B/P/M.

**Где в коде.** `CreatorMintCoordinator.swift`, `ODPMintJournal`: новое состояние «предложение мультиподписи неисполнимо».
Как приложение узнаёт о статусе предложения Safe — открытый вопрос (через WalletConnect Safe{Wallet} возвращает `safeTxHash`, а не хеш транзакции).

**Как проверить.** Локальный тест по образцу `chain/deploy/test/ODP07Safe.test.js`: предложение до полуночи UTC, подпись после
смены месяца, откат GS013, приложение показывает «неисполнимо», отклоняющая транзакция освобождает nonce, переподготовка с тем же `operationId` проходит.

---

## 11. Формат `.odpass` и импорт

**Что изменилось.** В архиве появляется файл копии фото. Остальное — как в `redesign-7`.

**Требование.** SPEC §15, CA-9, CA-13; [ODP_07_APP_HANDOFF.md](ODP_07_APP_HANDOFF.md), раздел 3; `schema/bundle-0.7/README.md`.

1. В корне точно `passport.json`, `generation.json`, `receipt.json`, `manifest.json`. Файлы — `files/<64 hex sha256>`.
2. `generation.json` перечисляет все девять спутников, отсутствующие роли явно. `abiGeneration` = `0.7-redesign-8`.
3. `receipt.json` содержит `operationId`, `transactionHash`, `blockNumber`, `blockHash`.
4. Копия фото, если есть, — обычный payload в `files/`, в `manifest.json`.
5. Пределы импорта CA-13.2: 512 МиБ всего, 256 МиБ на запись, 1000 записей, сжатие не больше 100:1, JSON до 1 МиБ и глубина 64.

**Где в коде.** Сейчас архив кладёт файлы в `originals/image__primary.bin` и т. п. с полем `role`
(`apple-app/Backend/Shared/Minting/CreatorPassportDocument.swift`, строки ≈171–188). Переделать под `files/<sha256hex>`.
Импорт: `apple-app/Backend/Shared/Import/CreatorBundleInspection.swift`, `CreatorBundleLoader.swift`; `swift-core/Sources/ODPCore/Passport/OdpassBundle.swift`, `Support/ZipArchive.swift`.
Старые архивы приложения читать отдельно и честно называть старым форматом.

**Как проверить.** Экспорт проходит `node chain/tools/bundle.mjs`-проверку (через тест или скрипт) и эталонные векторы `npm run vectors`.
Злонамеренные ZIP из приёмки `ODP_07_APP_HANDOFF.md` отклоняются с конкретной причиной.

---

## 12. Сеть и поколение

**Что изменилось.** Решение окончательное: только Polygon mainnet (137). Amoy не используется.

**Требование.** SPEC §7, §22.12, CA-12.1–12.4.

1. Встроить одно поколение: chainId 137, реестр и все спутники. Принимать только его.
2. Поколение развёрнуто и утверждено (`chain/generations.json`, запись `approvedGenerations[0]`). `CreatorRegistryPolicy.approvedDeployment` нужно заполнить из этой записи — реестр, все спутники, хеши runtime, блоки — и принимать только её (CA-12.1). Адреса 0.6 и старой 0.7 не подставлять.
3. Не подставлять адрес 0.6 из `ODPChain.contract` (`0x012aC639…`) и старые адреса 0.7.
4. Адреса из архива, QR или ссылки только сравниваются со встроенными. Чужая сеть или реестр — «не паспорт ODP / не проверено».

**Где в коде.** `CreatorRegistryPolicy.swift`, `swift-core/Sources/ODPCore/Chain/ODPChain.swift`, `WalletConnectSender` в `TransactionSender.swift` (по умолчанию берёт `ODPChain.contract`).

**Как проверить.** Значения в `approvedDeployment` побайтно совпадают с `chain/generations.json`. Без него регистрация, выпуск и публикация честно пишут «Реестр ODP 0.7 ещё не подключён». Тест с фиктивным поколением на локальном узле проходит весь путь, затем один ручной выпуск на mainnet тестовым профилем владельца.

---

## 13. Что остаётся из прежнего задания без изменений

Не повторяю, только список. Всё — в [ODP_07_APP_HANDOFF.md](ODP_07_APP_HANDOFF.md) и SPEC §22.

- Журнал утверждений `ODPStatementJournal` и отзывы в спутниках (раздел 4 handoff, SPEC §13, CA-11).
- Проверка по частям, без общего «подлинный» (раздел 5 handoff, CA-16).
- Тиражи: подготовка → mint → open (раздел 2 handoff, §20, CA-7). Только для B; в приложении пока отложено.
- Авторство и активация (CA-3, CA-4). Каталоги организаций (CA-10).
- NFC исключён из текущей работы.

---

## Порядок работ

Оценка в рабочих днях для одного разработчика с Claude. Шаги 1–10 — релиз профиля C, дальше — следующий этап.

1. Сверить ABI `redesign-8`, `generations.json` и новые номера CA; записать diff ABI в `CONTINUE_HERE.md`. — 0,5 дня.
2. Переписать кодировщик mint и декодеры header/media/classification/`getCreator`/`getPassportReleaseState` в `swift-core` и `apple-app`, с векторами из `ethers`. — 3 дня.
3. Добавить `abiGeneration` в `CreatorRegistryPolicy` и убрать stop профиля из `ODPVerdict`. — 0,5 дня.
4. Добавить `operationId` в журнал выпуска: сохранение, повтор, `AlreadyCommitted`, `MintOperationConflict`, смена месяца. — 3–4 дня.
5. Сделать публичную копию фото: удаление GPS, JPEG ≤ 1 МиБ, превью, якорь `preview`, `previewHash`. — 2–3 дня.
6. Перевести `.odpass` на `files/<sha256hex>` и `bundle-0.7`, со старым форматом только для чтения. — 3–4 дня.
7. Добавить проверку `eth_getCode` для C и метод `personalSign`. — 1–2 дня.
8. Сделать экраны-предупреждения CA-18 и напоминание о резервной копии. — 2 дня.
9. Показать окно отзыва по роли и сделать шлюз печати после фиксации. — 2 дня.
10. Написать строгий парсер QR и ссылок издателя, сверку `.well-known/odp.json`. — 2–3 дня.
11. Прогнать ODPTests, Release-сборку и ручной путь physical/digital; обновить `CONTINUE_HERE.md`. — 2 дня.
12. Написать ANS-104 с векторами `arbundles` и `EVMPublicKeyCache`. — 3–4 дня.
13. Написать `TurboClient`: цена, бесплатный остаток, загрузка, пополнение POL, `x-paid-by`. — 2–3 дня.
14. Написать `IPFSAddress` и `PublicFetchVerifier` со списком шлюзов. — 1–2 дня.
15. Сделать экраны публикации, «Кто платит», пополнения и флаг скрытия оплаты; текст для Review Notes. — 2–3 дня.
16. Сделать «Запросить POL» и показ баланса. — 1–2 дня.
17. Сделать неисполнимые предложения Safe на границе месяца (вместе с B/P/M). — 2–3 дня.

Итого: релиз C (шаги 1–11) — примерно 21–26 дней; публикация и спонсор (12–16) — ещё 9–14; мультиподпись (17) — 2–3.

---

## Открытые вопросы

1. **Схема якоря `preview`.** Правило «один, только при primary» в схеме есть. Нужны ли ещё `mime` и размеры в `data`? Проверяет ли кто-то предел 1 048 576 байт и отсутствие GPS, кроме приложения?
2. **Эталонные векторы копии** закоммичены в `main` (`schema/vectors/physical-preview.*`, `operations-redesign-8.json`); брать их.
3. **Где хранить запись о публикации** (ID data item, шлюзы, время): в `.odpass` вне хеша, локально или записывать `ar://…` в `ODPHosting` (это отдельная транзакция с газом)?
4. **Теги data item.** Нужен ли стандартный тег вроде `ODP-Sha256: <hex>`, чтобы копию находили через GraphQL шлюзов ar.io? Это стоит закрепить в SPEC.
5. **Публикация `.odpass` целиком.** Она раскрывает оригиналы и почти всегда платная. Разрешать ли это в приложении или только передачу файлом?
6. **EIP-7702.** Аккаунт с делегированием имеет код `0xef0100…`. Считать ли его «мультиподписью» для запрета C, или разрешать?
7. **Ссылка спонсора.** Предложение раздела 6 — ссылка вида `odp://sponsor?…` с названием, адресом кошелька и адресом приёма. Формат закрепить вместе с форматом запроса.
8. **Статус предложения Safe.** Как приложение узнаёт, что предложение ждёт подписей или стало неисполнимым: через Safe Transaction Service (сторонний сервер) или только по цепочке? Порог предупреждения — 24 часа (CA-5.8).
9. **CA-18.6** исправлен: приложение только напоминает, что за резервную копию кошелька отвечает сам человек, и никогда не спрашивает секреты.
10. **QR профиля `odp://<profileId>`** в `ShareOverlayView.swift` не описан в CA-14. Оставить, убрать или описать?
11. **Массовое тестирование.** `review/logic-recheck-abi7/REPORT.ru.md`, п. 1: тестовые паспорта в production-поколении останутся навсегда. Amoy по решению не используется. Как тогда проводить тесты с живыми людьми до чистого deployment?
12. **Предел публичной копии — 1 МиБ**, решение владельца окончательное.
